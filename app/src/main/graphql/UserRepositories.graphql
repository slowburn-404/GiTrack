query UserRepositories($login: String!, $last: Int!) {
    user(login: $login) {
        repositories(last: $last) {
            nodes{
                databaseId
                name
                description
                stargazerCount
                createdAt
                owner {
                    login
                }
                labels(last: $last) {
                    nodes {
                        name
                    }
                }
                issues {
                    totalCount
                }
            }

        }
    }
}