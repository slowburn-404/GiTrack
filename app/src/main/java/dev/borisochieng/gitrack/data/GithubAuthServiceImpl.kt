package dev.borisochieng.gitrack.data

/*class GithubAuthServiceImpl(
    private val retrofit: Retrofit
) : GitHubAuthService {
    override fun getAccessToken(
        clientId: String,
        clientSecret: String,
        code: String,
        callback: Callback<AccessTokenResponse>
    ) {

        val call: Call<AccessTokenResponse> = retrofit.create(GitHubAuthService::class.java)
            .getAccessToken(clientId,clientSecret, code)

        call.enqueue(callback)

    }
}*/